<?php
/**
 * Site-specific hooks and filters for this store.
 *
 * Examples:
 * - add_filter( 'woocommerce_product_tabs', 'my_custom_tabs' );
 * - add_action( 'wp_footer', 'my_tracking_script' );
 *
 * @package Lastify_Child
 */

defined( 'ABSPATH' ) || exit;
